<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/accent" />
    <corners android:radius="24dp" />
    <gradient
        android:angle="45"
        android:startColor="#3B82F6"
        android:centerColor="#60A5FA"
        android:endColor="#3B82F6"
        android:type="linear" />
    <stroke
        android:width="2dp"
        android:color="#80FFFFFF" />
    <!-- Padding to simulate glow via shadow in elevation, but XML shape glow is limited. 
         We rely on the gradient brightness and elevation on the view. -->
</shape>
